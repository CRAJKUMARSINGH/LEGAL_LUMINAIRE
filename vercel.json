{
  "buildCommand": "cd artifacts/legal-luminaire && npm install --ignore-scripts && npm run build",
  "outputDirectory": "artifacts/legal-luminaire/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}

