# ============================================================================
# Legal Luminaire — Netlify Production Config
#
# Netlify UI: Base directory = (blank/empty)  ← MUST be blank
# Build runs from repo root. pnpm activated via corepack (Node 22 image).
# ============================================================================

[build]
  command = "corepack enable && corepack prepare pnpm@10 --activate && pnpm install --no-frozen-lockfile --ignore-scripts=false && pnpm --filter @workspace/legal-luminaire run build"
  publish = "artifacts/legal-luminaire/dist/public"

[build.environment]
  NODE_VERSION    = "22"
  PNPM_VERSION    = "10"
  # Force --no-frozen-lockfile for ALL pnpm invocations including Netlify's
  # automatic pre-build install step (frozen-lockfile fails because lockfile
  # was generated on Windows; ignoredOptionalDependencies differs on Linux).
  PNPM_FLAGS      = "--no-frozen-lockfile"
  NPM_FLAGS       = "--version"
  CI              = "true"

# SPA catch-all — every client-side route returns index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options        = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy        = "strict-origin-when-cross-origin"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Week 13 — Kiro Foundation Lock — Content-Security-Policy (additive)
# unsafe-inline required for Vite injected HMR scripts + Tailwind runtime styles.
# frame-ancestors 'none' reinforces X-Frame-Options: DENY above.
# connect-src allows FastAPI backend proxy and local HMR websocket.
[[headers]]
  for = "/*"
  [headers.values]
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self'; connect-src 'self' http://127.0.0.1:8000 ws://localhost:*; frame-ancestors 'none';"
