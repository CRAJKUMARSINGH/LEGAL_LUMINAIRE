# Use repository-root netlify.toml for Netlify Connect (pnpm workspace).
# Vite outDir is dist/public — see vite.config.ts.

[build]
  command = "pnpm run build"
  publish = "dist/public"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
