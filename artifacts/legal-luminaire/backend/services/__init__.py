"""
backend/services/ — deterministic computation layer (Week 9)

Modules:
  limitation_engine   — pure-function deadline computation from rule table
  limitation_rules    — JSON rule table loader (versioned)

No LLM calls anywhere in this package.
"""
